# Raja
This Java program is a console-based railway reservation system that interacts with a MySQL database to manage PNR records. The program allows the user to:  Insert a new PNR record.  Delete an existing PNR record.  Display all PNR records.  Exit the program.  It uses JDBC 
